import { LoadingIconDirective } from './loading-icon.directive';

describe('LoadingIconDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingIconDirective();
    expect(directive).toBeTruthy();
  });
});
